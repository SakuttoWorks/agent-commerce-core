# --- Core Engine Dependencies (Agent-Commerce-OS) ---

# --- Web Framework & Server ---
# "standard" includes uvicorn and optional dependencies for production
fastapi[standard]>=0.115.0
uvicorn>=0.30.0

# --- Data Validation & Schema ---
# Version 2.10+ required for ConfigDict and strict type enforcement
pydantic>=2.10.0
pydantic-settings>=2.4.0

# --- AI & Search Integration ---
# Orchestration layer for LLM interactions
litellm>=1.81.0
# Search API for external data retrieval (public data normalization)
tavily-python>=0.7.0
google-generativeai>=0.8.0

# --- Infrastructure & State Management ---
upstash-redis>=1.6.0
supabase>=2.11.0

# --- Utilities ---
python-dotenv>=1.0.0
# Requires libmagic1 system dependency (see Dockerfile)
python-magic>=0.4.27
